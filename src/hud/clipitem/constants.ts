export const CLIP_ITEM_HUD_BORDER_RUN_DURATION = {
  min: 0.6,
  max: 8,
  defaultValue: 2.4,
} as const;

export const CLIP_ITEM_HUD_BORDER_RING_WIDTH = {
  min: 1,
  max: 6,
  defaultValue: 2,
} as const;
